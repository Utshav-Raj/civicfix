import { ComingSoon } from "@/components/dashboard/coming-soon";

export default function MapPage() {
  return (
    <ComingSoon
      title="Map View"
      description="A full-screen, clusterable map of every issue in your city — with heatmap overlay, layer toggles, and filtered hotspots. Coming in Phase 3."
    />
  );
}

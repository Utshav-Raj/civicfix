import { ComingSoon } from "@/components/dashboard/coming-soon";

export default function MyReportsPage() {
  return (
    <ComingSoon
      title="My Reports"
      description="A dedicated view of every issue you've submitted, with live status, comment threads, and resolution timers — landing in Phase 3 with the Supabase wiring."
    />
  );
}
